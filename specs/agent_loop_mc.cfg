\* TLC Model Checking Configuration for agent_loop.tla
\* Layer 1: Single-session model

SPECIFICATION Spec

\* Constants -- small values to keep state space feasible
CONSTANTS
    Callers = {c1, c2, c3}
    MaxTurns = 2
    MaxToolCalls = 2

\* Safety invariants checked in every reachable state
INVARIANTS
    TypeInvariant
    SafetyInvariant

\* Liveness properties (temporal)
PROPERTIES
    TurnTermination
    CancellationResponsiveness
    CloseTermination
    ReceiverDrain
